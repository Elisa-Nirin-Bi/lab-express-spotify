<div class='albums'>
  {{#each albums}}
    <div class="album">
        <h2>{{this.name}}</h2>
      <img class="artistAlbumImg" src='{{images.0.url}}' alt={{name}}/>
   <a href='/tracks/{{id}}'><button>View Tracks</button></a>  
    </div>
  {{/each}}
</div>